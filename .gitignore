node_modules/
node_modules

.agents/
.cursor/

.windsurf/

dist/
coverage/

# Environment variables — never commit real secrets/config.
# .env.example is the template; copy it to .env locally (see README).
.env
.env.local
.env.*.local

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS files
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/

# TypeScript incremental build info
*.tsbuildinfo

# Deploy CLI local config (created by `vercel link` / `railway link`)
.vercel
.railway
