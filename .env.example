# Database Configuration
DATABASE_URL=postgresql://user:password@localhost:5432/openplanpoker

# Server Configuration
PORT=3000
NODE_ENV=development

# Comma-separated list of extra origins allowed to call the API / connect via WebSocket
# (e.g. your Vercel frontend URL). Not needed for local dev.
# ALLOWED_ORIGINS=https://your-app.vercel.app

# Frontend Configuration
VITE_API_BASE_URL=http://localhost:3000/api
VITE_SOCKET_URL=http://localhost:3000
